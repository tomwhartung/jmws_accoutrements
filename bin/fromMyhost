#!/bin/bash
#
# fromMyhost: scp specified file FROM corresponding dir on myhost
# ---------------------------------------------------------------
#
PATH=/var/www/jmws_accoutrements/bin/libs/bash:${PATH}
remote_host_from myhost $@
exit_ret_val=$?

exit $exit_ret_val
