#!/bin/bash
#
# toMyhost: scp specified file to appropriate dir on myhost
# ---------------------------------------------------------
#
PATH=/var/www/jmws_accoutrements/bin/libs/bash:${PATH}
remote_host_to myhost $@
exit_ret_val=$?

exit $exit_ret_val
